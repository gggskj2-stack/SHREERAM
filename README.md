# Telegram Course Uploader Bot

Telegram bot jo courses upload karta hai text file se.

## Setup

1. Git clone karo.
2. pip install -r requirements.txt
3. src/bot.py run karo.

## Deployment on Render
Render par deploy karne ke steps niche diye hain.
